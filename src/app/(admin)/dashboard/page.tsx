/**
 * Dashboard do gestor. Placeholder da Fase 0 — métricas reais (lendo de
 * `daily_sales`) chegam na Fase 6.
 */
export default function AdminDashboard() {
  return (
    <main className="mx-auto flex min-h-dvh max-w-2xl flex-col items-center justify-center gap-4 px-6 text-center">
      <h1 className="font-display text-3xl font-bold">Dashboard</h1>
      <p className="text-sm text-muted-foreground">
        Métricas de venda chegam na Fase 6.
      </p>
    </main>
  );
}
